var React = require('react');

var Home = React.createClass({
  render: function() {
    return (
      <div> This be the main content that will switch out with routing </div>
    );
  }
});

module.exports = Home;
